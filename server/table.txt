id as FK
transaction_id as int
transaction_date as date
amount as double


CREATE TABLE transactions (
  transaction_id INTEGER PRIMARY KEY,
  amount DOUBLE,
  expense_name STRING,
  transaction_date DATETIME defaultcurrent_timestamp,
  FOREIGN KEY (id) REFERENCES users(id)
)